# Kopieren nach .env und Werte eintragen — .env bleibt lokal (gitignored)
PVUEB_INVERTER_IP=

# Optional: OCPP-Heartbeat der Wallbox in Sekunden (Default 10, im Web-UI änderbar)
#PVUEB_HEARTBEAT_S=10
