# Umgebungsspezifisches / Geheimnisse — bleibt lokal
.env
.env.bak
.env.tmp
LOCAL.md

# Reste der entfernten MyAudi-Anbindung: Token und Antwort-Cache enthalten
# Zugangsdaten. Die Dateien können gelöscht werden; bis dahin bleiben sie
# gesperrt, damit sie nicht versehentlich doch im Repository landen.
poc/.audi-tokenstore.json
poc/.audi-cache.json

# Sitzungssicherung für den Neustart (Laufzeitzustand, kein Konfigurationswert)
poc/.session.json

# Python
.venv/
__pycache__/
*.pyc

# Logs
*.log

# Live-Mitschnitte des Status (Rohmaterial für Testfälle)
recordings/
